spring.application.name=adminportal
# src/main/resources/application.properties

# DB config
spring.datasource.url=jdbc:mysql://localhost:3306/Azkafashion
spring.datasource.username=root
spring.datasource.password=nasir786..

# Hibernate/JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Static resources config
spring.web.resources.static-locations=classpath:/static/,file:uploads/

# Allow larger file uploads (example: 10MB)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Server port and context-path (optional)
server.port=8080
# Enable CORS for all origins
spring.web.resources.add-mappings=true
spring.web.cors.allowed-origins=http://127.0.0.1:5500


